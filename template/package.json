{
  "name": "mcp-server",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:ui": "vite build",
    "build:server": "tsc --outDir dist",
    "build:zip": "cp package.json package-lock.json dist/ && cd dist && zip -r ../dist/dist.zip . && cd ..",
    "build": "npm run build:ui && npm run build:server && npm run build:zip",
    "dev": "node --import tsx src/index.ts",
    "inspect": "node --import tsx src/index.ts & sleep 1 && mcp-inspector --transport streamablehttp http://localhost:${PORT:-3000}",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@modelcontextprotocol/inspector": "^0.21.1",
    "@types/node": "^25.5.0",
    "tsx": "^4.21.0",
    "typescript": "^5",
    "vite": "^7.3.1",
    "vite-plugin-singlefile": "^2.3.0"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.1007.0",
    "@modelcontextprotocol/ext-apps": "^1.2.2",
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^4.3.6"
  }
}
